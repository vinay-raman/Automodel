# Copyright (c) 2026, NVIDIA CORPORATION. All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Validate recipe configs

on:
  pull_request:
    paths:
      - 'tests/ci_tests/configs/**'
      - 'examples/**/*.yaml'
      - 'tests/ci_tests/utils/validate_nightly_recipes.py'
      - 'tests/ci_tests/utils/validate_generate_ci.py'
      - 'tests/ci_tests/utils/validate_new_recipe_ci.py'
      - 'tests/ci_tests/utils/generate_ci_tests.py'
      - 'tools/lint_example_yamls.py'

jobs:
  validate-nightly-recipes:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.10"

      - name: Set up UV
        uses: astral-sh/setup-uv@v1
        with:
          version: 0.8.22

      - name: Install validator deps
        run: |
          uv venv .venv-validator
          uv pip install --python .venv-validator/bin/python --quiet pyyaml ruamel.yaml

      - name: Lint example YAMLs
        run: .venv-validator/bin/python tools/lint_example_yamls.py --automodel-dir .

      - name: Validate nightly recipe references
        run: .venv-validator/bin/python tests/ci_tests/utils/validate_nightly_recipes.py --automodel-dir .

      - name: Validate generate_ci_tests across all (folder, scope) combos
        run: .venv-validator/bin/python tests/ci_tests/utils/validate_generate_ci.py --automodel-dir .

      - name: Validate ci section on newly added recipes
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          # Diff all of examples/; the validator filters by suffix and CI-enablement.
          added=$(git diff --name-only --diff-filter=A "$BASE_SHA...$HEAD_SHA" -- examples/)
          if [ -z "$added" ]; then
            echo "No newly added files under examples/ in this PR; skipping."
            exit 0
          fi
          echo "$added"
          echo "$added" | xargs .venv-validator/bin/python tests/ci_tests/utils/validate_new_recipe_ci.py --automodel-dir .
